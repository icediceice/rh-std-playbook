{
  "name": "Flush VPN Phase 1 Tunnel Survey",
  "description": "Survey for customizing Phase 1 tunnel flush variables (host list is managed separately).",
  "questions": [
    { "variable": "vpn_name", "type": "text", "question": "Enter VPN name to flush Phase 1 tunnels for:", "default": "vpn1" },
    { "variable": "vdom", "type": "text", "question": "Enter VDOM name:", "default": "root" },
    { "variable": "force_tunnel_online", "type": "multiplechoice", "question": "Force tunnels online after flush?", "choices": ["true", "false"], "default": "true" },
    { "variable": "force_tunnel_max_retries", "type": "integer", "question": "Maximum force tunnel retry attempts:", "default": 3, "min": 1, "max": 10 }
  ]
}
