{
  "name": "Flush Tunnel 1 Survey",
  "description": "Survey for customizing Tunnel 1 flush variables (host list is managed separately).",
  "questions": [
    { "variable": "tunnel_name", "type": "text", "question": "Enter tunnel name:", "default": "tunnel1" },
    { "variable": "vdom", "type": "text", "question": "Enter VDOM name:", "default": "root" }
  ]
}
